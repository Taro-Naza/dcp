type User = {
    firstName: String;
    lastName: String;
    email: String;
    password: String;
    status: Boolean;
    isAdmin: Boolean;
    createdAt: Date;
    updatedAt: Date;
};

export default User;
